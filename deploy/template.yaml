apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: platform-frontend-ai-dev
parameters:
- name: NAMESPACE
  value: ${NAMESPACE}
- name: MEMORY_SERVER_IMAGE
  value: quay.io/redhat-services-prod/hcc-platex-services/platform-frontend-ai-dev-memory-server
- name: PROXY_IMAGE
  value: quay.io/redhat-services-prod/hcc-platex-services/platform-frontend-ai-dev-proxy
- name: MEMORY_SERVER_IMAGE_TAG
  required: true
- name: PROXY_IMAGE_TAG
  required: true
- name: GCP_PROJECT_ID
  required: true
- name: GCP_REGION
  required: true
- name: VERTEX_ALLOWED_MODELS
  required: true
# Empty default: existing deploys keep working before the Vault key exists.
# The API key itself is never a parameter — secret only.
- name: OPENAI_ALLOWED_MODELS
  value: ""
- name: PROXY_NAME
  value: devbot-proxy
# Allow the HCC Framework runner to reach the shared OpenAI gateway. Must match
# BOT_NAME in its instance deployment; other bots remain unchanged by default.
- name: OPENAI_EGRESS_BOT_NAME
  value: devbot-framework
- name: JIRA_SECRET_NAME
  value: devbot-secrets
- name: GIT_AUTH_ENABLED
  value: "false"
- name: GITLAB_TLS_SKIP_VERIFY
  value: "false"
- name: DASHBOARD_URL
  description: public hostname for the memory server dashboard
  required: true
objects:

# ============================================================================
# Shared infrastructure for dev-bot instances (memory-server + proxy).
# Bot pods are deployed by instance-specific repos/templates.
#
# Prerequisites:
#
# 1. RDS instance with the pgvector extension enabled
# 2. Secret "devbot-secrets" (Vault) with the following keys:
#      bot-gh-username       — GitHub username / git name
#      bot-email             — GitHub email for git commits
#      bot-gl-name           — GitLab username / git name
#      bot-gl-email          — GitLab email for git commits
#      gh-bot-gpg-private    — GPG private key (base64)
#      gh-bot-cli-token      — GitHub personal access token
#      gl-bot-cli-token      — GitLab personal access token
#      gitlab-ca-cert-b64    — (optional) base64 PEM bundle for internal GitLab CAs
#      gcp-vertex-claude-sa  — Google service account key (base64)
#      jira-email            — Jira username/email (unless JIRA_SECRET_NAME overrides)
#      jira-token            — Jira API token (unless JIRA_SECRET_NAME overrides)
#      upload-memory-password — (temporary) shared secret for bulk memory upload
#      konflux-read-token    — (optional) KubeArchive SA token for Konflux build logs
#      glitchtip-url         — GlitchTip instance URL (e.g. https://glitchtip.devshift.net)
#      glitchtip-token       — GlitchTip API token for error tracking
#      openai-api-key        — OpenAI API key for the :8450 gateway.
#                              Proxy-only; the :8450 listener stays off until it exists.
# 3. (Optional) Separate Jira secret (JIRA_SECRET_NAME) with keys:
#      jira-email            — Jira username/email for this instance
#      jira-token            — Jira API token for this instance
# 4. ServiceAccount "devbot-memory-server" with oauth-redirecturi annotation
#      (requires DASHBOARD_URL parameter matching the Route hostname)
# 5. Secret "devbot-secrets" with key session_secret
#      (provision via Vault in app-interface namespace YAML)
#
# ============================================================================

# --- Memory Server ServiceAccount (OpenShift OAuth client) ---
- kind: ServiceAccount
  apiVersion: v1
  metadata:
    name: devbot-memory-server
    labels:
      app.kubernetes.io/name: memory-server
      app.kubernetes.io/part-of: devbot
    annotations:
      serviceaccounts.openshift.io/oauth-redirecturi.dashboard: 'https://${DASHBOARD_URL}/oauth/callback'

# --- OAuth proxy API delegation ---
- apiVersion: rbac.authorization.k8s.io/v1
  kind: ClusterRoleBinding
  metadata:
    name: devbot-memory-server-auth-delegator
  subjects:
  - kind: ServiceAccount
    name: devbot-memory-server
    namespace: ${NAMESPACE}
  roleRef:
    kind: ClusterRole
    name: system:auth-delegator
    apiGroup: rbac.authorization.k8s.io

# --- Memory Server Deployment ---
- apiVersion: apps/v1
  kind: Deployment
  metadata:
    name: devbot-memory-server
    labels:
      app.kubernetes.io/name: memory-server
      app.kubernetes.io/part-of: devbot
  spec:
    replicas: 1
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 0
        maxSurge: 1
    selector:
      matchLabels:
        app.kubernetes.io/name: memory-server
    template:
      metadata:
        labels:
          app.kubernetes.io/name: memory-server
          app.kubernetes.io/part-of: devbot
      spec:
        serviceAccountName: devbot-memory-server
        volumes:
        - name: oauth-proxy-cookie-secret
          secret:
            secretName: devbot-secrets
            items:
            - key: session_secret
              path: session_secret
        containers:
        - name: oauth-proxy
          image: registry.redhat.io/openshift4/ose-oauth-proxy-rhel9:v4.18.0-202506230505.p0.gcbd44ad.assembly.stream.el9
          ports:
          - containerPort: 8443
            name: dashboard
            protocol: TCP
          args:
          - -provider=openshift
          - -https-address=
          - -http-address=:8443
          - -upstream=http://localhost:8080
          - -openshift-ca=/var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          - '--openshift-sar={"resource": "projects", "verb": "list"}'
          - '--openshift-delegate-urls={"/": {"verb": "list", "resource": "projects"}}'
          - --cookie-secret-file=/etc/secrets/session_secret
          - --cookie-samesite=none
          - --client-id=system:serviceaccount:${NAMESPACE}:devbot-memory-server
          - --client-secret-file=/var/run/secrets/kubernetes.io/serviceaccount/token
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          volumeMounts:
          - name: oauth-proxy-cookie-secret
            mountPath: /etc/secrets
            readOnly: true
        - name: memory-server
          image: ${MEMORY_SERVER_IMAGE}:${MEMORY_SERVER_IMAGE_TAG}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop: ["ALL"]
          ports:
          - containerPort: 8080
            name: http
            protocol: TCP
          - containerPort: 9091
            name: metrics
            protocol: TCP
          env:
          - name: PGSQL_HOSTNAME
            valueFrom:
              secretKeyRef:
                name: platform-frontend-ai-dev-rds
                key: db.host
          - name: PGSQL_PORT
            valueFrom:
              secretKeyRef:
                name: platform-frontend-ai-dev-rds
                key: db.port
          - name: PGSQL_USER
            valueFrom:
              secretKeyRef:
                name: platform-frontend-ai-dev-rds
                key: db.user
          - name: PGSQL_PASSWORD
            valueFrom:
              secretKeyRef:
                name: platform-frontend-ai-dev-rds
                key: db.password
          - name: PGSQL_DATABASE
            valueFrom:
              secretKeyRef:
                name: platform-frontend-ai-dev-rds
                key: db.name
          - name: PGSQL_SSLMODE
            value: require
          - name: UPLOAD_MEMORY_PASSWORD
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: upload-memory-password
                optional: true
          - name: KUBEARCHIVE_TOKEN
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: konflux-read-token
                optional: true
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 2Gi
            limits:
              cpu: 500m
              memory: 4Gi

# --- Memory Server Service ---
- apiVersion: v1
  kind: Service
  metadata:
    name: devbot-memory-server
    labels:
      app.kubernetes.io/name: memory-server
      app.kubernetes.io/part-of: devbot
  spec:
    type: ClusterIP
    selector:
      app.kubernetes.io/name: memory-server
    ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    - port: 9091
      targetPort: metrics
      protocol: TCP
      name: metrics
    - port: 8443
      targetPort: 8443
      protocol: TCP
      name: dashboard

# --- Memory Server Route (dashboard behind oauth-proxy SSO) ---
- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    name: devbot-memory-server
    labels:
      app.kubernetes.io/name: memory-server
      app.kubernetes.io/part-of: devbot
  spec:
    host: ${DASHBOARD_URL}
    to:
      kind: Service
      name: devbot-memory-server
    port:
      targetPort: dashboard
    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect

# --- Proxy Deployment (Squid + Executor server — holds all CLI secrets) ---
- apiVersion: apps/v1
  kind: Deployment
  metadata:
    name: ${PROXY_NAME}
    labels:
      app.kubernetes.io/name: ${PROXY_NAME}
      app.kubernetes.io/part-of: devbot
  spec:
    replicas: 1
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 0
        maxSurge: 1
    selector:
      matchLabels:
        app.kubernetes.io/name: ${PROXY_NAME}
    template:
      metadata:
        labels:
          app.kubernetes.io/name: ${PROXY_NAME}
          app.kubernetes.io/part-of: devbot
      spec:
        containers:
        - name: proxy
          image: ${PROXY_IMAGE}:${PROXY_IMAGE_TAG}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop: ["ALL"]
          ports:
          - containerPort: 3128
            name: squid
            protocol: TCP
          - containerPort: 9090
            name: executor
            protocol: TCP
          - containerPort: 8443
            name: vertex-auth
            protocol: TCP
          - containerPort: 8444
            name: jira-mcp
            protocol: TCP
          - containerPort: 8446
            name: screenshot
            protocol: TCP
          - containerPort: 8447
            name: git-auth
            protocol: TCP
          - containerPort: 8448
            name: glitchtip
            protocol: TCP
          - containerPort: 8450
            name: openai-auth
            protocol: TCP
          - containerPort: 9091
            name: metrics
            protocol: TCP
          env:
          - name: EXECUTOR_LISTEN
            value: ":9090"
          - name: GH_TOKEN
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: gh-bot-cli-token
          - name: GH_USERNAME
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: bot-gh-username
          - name: GITLAB_TOKEN
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: gl-bot-cli-token
          - name: GL_USERNAME
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: bot-gl-name
          - name: GPG_PRIVATE_KEY_B64
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: gh-bot-gpg-private
          - name: GOOGLE_SA_KEY_B64
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: gcp-vertex-claude-sa
          - name: GCP_PROJECT_ID
            value: ${GCP_PROJECT_ID}
          - name: GCP_REGION
            value: ${GCP_REGION}
          - name: VERTEX_ALLOWED_MODELS
            value: ${VERTEX_ALLOWED_MODELS}
          - name: OPENAI_API_KEY
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: openai-api-key
                optional: true
          - name: OPENAI_ALLOWED_MODELS
            value: ${OPENAI_ALLOWED_MODELS}
          - name: JIRA_URL
            value: https://redhat.atlassian.net
          - name: JIRA_USERNAME
            valueFrom:
              secretKeyRef:
                name: ${JIRA_SECRET_NAME}
                key: jira-email
          - name: JIRA_API_TOKEN
            valueFrom:
              secretKeyRef:
                name: ${JIRA_SECRET_NAME}
                key: jira-token
          - name: GLITCHTIP_URL
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: glitchtip-url
          - name: GLITCHTIP_TOKEN
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: glitchtip-token
          - name: GIT_AUTH_ENABLED
            value: ${GIT_AUTH_ENABLED}
          - name: GITLAB_TLS_SKIP_VERIFY
            value: ${GITLAB_TLS_SKIP_VERIFY}
          - name: GITLAB_CA_CERT_B64
            valueFrom:
              secretKeyRef:
                name: devbot-secrets
                key: gitlab-ca-cert-b64
                optional: true
          # HTTP /metrics avoids Squid logging TCP kubelet probes as
          # DENIED error:transaction-end-before-headers (TCP:3128).
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 512Mi

# --- Proxy Service ---
- apiVersion: v1
  kind: Service
  metadata:
    name: ${PROXY_NAME}
    labels:
      app.kubernetes.io/name: ${PROXY_NAME}
      app.kubernetes.io/part-of: devbot
  spec:
    type: ClusterIP
    selector:
      app.kubernetes.io/name: ${PROXY_NAME}
    ports:
    - port: 3128
      targetPort: squid
      protocol: TCP
      name: squid
    - port: 9090
      targetPort: executor
      protocol: TCP
      name: executor
    - port: 8443
      targetPort: vertex-auth
      protocol: TCP
      name: vertex-auth
    - port: 8444
      targetPort: jira-mcp
      protocol: TCP
      name: jira-mcp
    - port: 8446
      targetPort: screenshot
      protocol: TCP
      name: screenshot
    - port: 8447
      targetPort: git-auth
      protocol: TCP
      name: git-auth
    - port: 8448
      targetPort: glitchtip
      protocol: TCP
      name: glitchtip
    - port: 8450
      targetPort: openai-auth
      protocol: TCP
      name: openai-auth
    - port: 9091
      targetPort: metrics
      protocol: TCP
      name: metrics

# --- NetworkPolicy: Proxy accepts ingress from bot pods ---
- apiVersion: networking.k8s.io/v1
  kind: NetworkPolicy
  metadata:
    name: ${PROXY_NAME}-ingress
    labels:
      app.kubernetes.io/part-of: devbot
  spec:
    podSelector:
      matchLabels:
        app.kubernetes.io/name: ${PROXY_NAME}
    policyTypes:
    - Ingress
    ingress:
    - from:
      - podSelector:
          matchLabels:
            app.kubernetes.io/part-of: devbot
      ports:
      - port: 3128
        protocol: TCP
      - port: 9090
        protocol: TCP
      - port: 8443
        protocol: TCP
      - port: 8444
        protocol: TCP
      - port: 8446
        protocol: TCP
      - port: 8447
        protocol: TCP
      - port: 8448
        protocol: TCP
      - port: 8450
        protocol: TCP
    - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: openshift-customer-monitoring
      ports:
      - port: 9091
        protocol: TCP

# --- NetworkPolicy: Selected runner may reach the OpenAI model gateway ---
- apiVersion: networking.k8s.io/v1
  kind: NetworkPolicy
  metadata:
    name: ${PROXY_NAME}-openai-egress
    labels:
      app.kubernetes.io/part-of: devbot
  spec:
    podSelector:
      matchLabels:
        app.kubernetes.io/name: ${OPENAI_EGRESS_BOT_NAME}
    policyTypes:
    - Egress
    egress:
    - to:
      - podSelector:
          matchLabels:
            app.kubernetes.io/name: ${PROXY_NAME}
      ports:
      - port: 8450
        protocol: TCP

# --- NetworkPolicy: Memory server accepts ingress from bot pods + OpenShift router ---
- apiVersion: networking.k8s.io/v1
  kind: NetworkPolicy
  metadata:
    name: devbot-memory-server-ingress
    labels:
      app.kubernetes.io/part-of: devbot
  spec:
    podSelector:
      matchLabels:
        app.kubernetes.io/name: memory-server
    policyTypes:
    - Ingress
    ingress:
    - from:
      - podSelector:
          matchLabels:
            app.kubernetes.io/part-of: devbot
      ports:
      - port: 8080
        protocol: TCP
    - from:
      - namespaceSelector:
          matchLabels:
            network.openshift.io/policy-group: ingress
      ports:
      - port: 8443
        protocol: TCP
    - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: openshift-customer-monitoring
      ports:
      - port: 9091
        protocol: TCP
